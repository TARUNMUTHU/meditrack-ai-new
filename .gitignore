# Node.js modules
node_modules/ # For root-level node_modules, if any
frontend/node_modules/ # CRITICAL for your frontend's node_modules
backend/node_modules/  # Add this if backend also has its own node_modules

npm-debug.log
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Sensitive files (CRITICAL: DO NOT COMMIT THESE)
*.key
*.pem
*.pfx
*.jks
*.cer
*.crt
*.p12
# Specific to your project:
backend/meditrack-vision-ai-service-account.json.json

# Frontend specific (Create React App builds and cache)
/frontend/build
/frontend/.cache # This targets frontend/.cache which is good

# Backend specific (uploads folder, logs, and optionally large data)
/backend/uploads/
/backend/logs/
# Add this line if you do NOT want backend/eng.traineddata to be committed:
# backend/eng.traineddata 

# IDE and OS specific files
.vscode/
.idea/
.DS_Store
Thumbs.db
ehthumbs.db
.directory
*.log
*.tmp
*.swp